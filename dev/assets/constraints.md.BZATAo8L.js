import{_ as a,o as t,c as n,ap as r}from"./chunks/framework.Dv1_Li4I.js";const u=JSON.parse('{"title":"Core constraints","description":"","frontmatter":{},"headers":[],"relativePath":"constraints.md","filePath":"constraints.md","lastUpdated":null}'),i={name:"constraints.md"};function o(s,e,l,c,d,p){return t(),n("div",null,[...e[0]||(e[0]=[r('<h1 id="Core-constraints" tabindex="-1">Core constraints <a class="header-anchor" href="#Core-constraints" aria-label="Permalink to &quot;Core constraints {#Core-constraints}&quot;">​</a></h1><p>These constraints apply to every ReactiveKernels lowering, extension, and example package. They also apply when data is bound during preparation. Backend limitations must be reported explicitly; they do not relax these rules.</p><h2 id="Preserve-data-dependent-iteration" tabindex="-1">Preserve data-dependent iteration <a class="header-anchor" href="#Preserve-data-dependent-iteration" aria-label="Permalink to &quot;Preserve data-dependent iteration {#Preserve-data-dependent-iteration}&quot;">​</a></h2><p><strong>Do not statically unroll a loop whose trip count derives from data.</strong> This includes runtime inputs, bound arrays, shapes, subject or observation counts, operation tables, ragged ranges, dose counts, and capacities computed from any of them. Knowing a length at preparation or trace time does not make it a structural constant. For example, expanding one statement per bit of the largest bound dose count is forbidden just as expanding one statement per subject is.</p><p>Retain a runtime loop, scan, or equivalent backend control-flow operation with explicit carried state and output buffers. Shapes may specialize an executable; the lowering must not duplicate the body according to those shapes or values. Native execution retains ordinary iteration. Fixed structural algebra, such as the scalar entries of an intrinsically three-compartment operator, is distinct from data-derived iteration and may be expanded.</p><h2 id="Preserve-lazy-branches" tabindex="-1">Preserve lazy branches <a class="header-anchor" href="#Preserve-lazy-branches" aria-label="Permalink to &quot;Preserve lazy branches {#Preserve-lazy-branches}&quot;">​</a></h2><p><strong>Do not replace required lazy control flow with eager <code>ifelse</code>, masked evaluation of both branches, or equivalent predication to bypass a compiler failure.</strong> An inactive branch must remain inactive, including its indexing, allocation, mutation, potentially undefined arithmetic, and derivative work. Clamping indices or inventing dummy buffers does not make such a rewrite valid.</p><p>Ordinary elementwise selection between already valid values remains a selection; it does not authorize evaluating an otherwise inactive computation. Preserve the authored Julia branch and loop semantics in both primal and derivative execution. If a backend cannot express them, report the unsupported case and isolate the backend failure instead of adopting an eager workaround.</p><h2 id="Acceptance-and-existing-limitations" tabindex="-1">Acceptance and existing limitations <a class="header-anchor" href="#Acceptance-and-existing-limitations" aria-label="Permalink to &quot;Acceptance and existing limitations {#Acceptance-and-existing-limitations}&quot;">​</a></h2><p>A lowering change must demonstrate that increasing relevant data lengths or capacities does not replicate loop bodies or control-flow regions. Check the generated backend structure as well as primal and AD parity with native Julia, including inactive branches and empty or ragged cases where supported. A small Julia statement count alone does not establish this: tracing can still expand a host loop.</p><p>These are required constraints, not a claim that every existing path already conforms. The known residual gap is the bounded stateful path kept for a method whose control flow surrounds a host-drained observational callable; the <a href="./compiler">compiler</a> page describes it. Such paths require retained control flow or explicit rejection; their current behavior is not an exception or a template for new implementations. The Reactant <a href="./scan">scan</a> lowering retains one <code>while</code> loop for every iterated-sequence shape, including bound host sequences.</p><p>The experimental rectangular PK path retains its loops and lazy branches but still fails reverse compilation. Its eager-branch and data-derived unrolling workarounds are not acceptable fixes. See the <a href="./scan">scan limitations</a>.</p><p>Two further backend limitations are isolated with standalone reproducers under <code>benchmark/</code> (Reactant and its AD engine only, no ReactiveKernels code):</p><ul><li><p>A lazy branch inside a batched plate cell compiles and evaluates for every lane count, but reverse compilation through it fails once the batching pass realizes the plate as a loop (six lanes fail where four lanes, unrolled per lane, succeed): <code>repro_reactant_batch_if_reverse.jl</code>. Plated support guards therefore keep their authored branch and lose Reactant reverse gradients above that size until the batched-loop branch lowers upstream; native execution and native reverse are unaffected.</p></li><li><p>Reverse compilation through a retained <code>while</code> loop whose exit is data dependent (the adaptive ODE solver&#39;s <code>(n &lt; maxiters) &amp; (t &lt; t1)</code>) fails because the loop has no statically known iteration count: <code>repro_reactant_adaptive_while_reverse.jl</code>. The solver keeps the retained loop; its supported gradient is the backsolve adjoint, which differentiates only the loop-free right-hand side.</p></li></ul>',14)])])}const m=a(i,[["render",o]]);export{u as __pageData,m as default};
