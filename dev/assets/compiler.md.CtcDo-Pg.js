import{_ as i,C as n,ap as s,o as l,c as d,j as e,a,E as c,w as p,a2 as u,ao as h}from"./chunks/framework.DM2KPRmX.js";const k=JSON.parse('{"title":"Compiler capability and limits","description":"","frontmatter":{},"headers":[],"relativePath":"compiler.md","filePath":"compiler.md","lastUpdated":null}'),m={name:"compiler.md"},g={class:"rk-result-assets"};function b(y,t,f,v,I,w){const r=n("ClientOnly"),o=s("exec-scripts");return l(),d("div",null,[t[0]||(t[0]=e("h1",{id:"Compiler-capability-and-limits",tabindex:"-1"},[a("Compiler capability and limits "),e("a",{class:"header-anchor",href:"#Compiler-capability-and-limits","aria-label":'Permalink to "Compiler capability and limits {#Compiler-capability-and-limits}"'},"​")],-1)),c(r,null,{default:p(()=>[u(e("div",g,null,512),[[o,""]])]),_:1}),t[1]||(t[1]=h('<p>ReactiveKernels has one small exact planner and several lowering surfaces built around it. This page is the capability contract: it says which decisions the compiler makes, which facts an author must supply, which work disappears from a prepared hot path, and which source shapes are deliberately rejected.</p><p>The word <em>compiler</em> is used here for three related but different pipelines:</p><ul><li><p>the public stateless have→want compiler, which selects recipes and emits a straight-line Julia callable;</p></li><li><p>the public reactive compiler, which fixes the same selected graph into typed slots, validity bits, dependency closures, and generated lazy getters; and</p></li><li><p>the source-captured state-machine compiler, which analyzes method-bearing <code>@kernel</code> definitions using captured and validated effect metadata. Its most complete consumer is the external sealed native NUTS acceptance artifact.</p></li></ul><p>Those surfaces share graph identities, producer selection, and currentness rules. They do <strong>not</strong> share the same public stability or source-language boundary. In particular, a stateless recipe may contain arbitrary ordinary Julia behind an opaque recipe call, while a source-captured state-machine method must stay inside the compiler&#39;s inspectable and registered subset.</p><h2 id="API-map" tabindex="-1">API map <a class="header-anchor" href="#API-map" aria-label="Permalink to &quot;API map {#API-map}&quot;">​</a></h2><ol class="rk-pipeline"><li class="rk-pipeline-step"><article><h3>Author a dataflow graph</h3><dl><dt>Public surface</dt><dd><code>@kernel, Graph, Value, Recipe, add!, merge, compose</code></dd><dt>Result</dt><dd>KernelSpec or Graph metadata</dd><dt>Runtime work that remains</dt><dd>None; authoring executes no recipe body</dd></dl></article></li><li class="rk-pipeline-step"><article><h3>Select work</h3><dl><dt>Public surface</dt><dd><code>plan, explain, inputs, outputs</code></dd><dt>Result</dt><dd>Inspectable Plan</dd><dt>Runtime work that remains</dt><dd>None after preparation</dd></dl></article></li><li class="rk-pipeline-step"><article><h3>Emit a stateless kernel</h3><dl><dt>Public surface</dt><dd><code>lower, transform, compile, prepare, prepare!, code_expr</code></dd><dt>Result</dt><dd>PreparedKernel</dd><dt>Runtime work that remains</dt><dd>Only selected recipe calls and their value flow</dd></dl></article></li><li class="rk-pipeline-step"><article><h3>Lift scalar work</h3><dl><dt>Public surface</dt><dd><code>plate, lower_batched, replica</code></dd><dt>Result</dt><dd>Batched or replicated callable</dd><dt>Runtime work that remains</dt><dd>One native loop/map or a backend tensor program</dd></dl></article></li><li class="rk-pipeline-step"><article><h3>Reuse recipe storage</h3><dl><dt>Public surface</dt><dd><code>prepare_nonallocating</code></dd><dt>Result</dt><dd>NonAllocatingKernel</dd><dt>Runtime work that remains</dt><dd>Selected calls plus cache mutation; no planner</dd></dl></article></li><li class="rk-pipeline-step"><article><h3>Plan demands dynamically</h3><dl><dt>Public surface</dt><dd><code>ReactiveState, get!, materialize!, freeze!, checkpoint</code></dd><dt>Result</dt><dd>Open-ended incremental state</dd><dt>Runtime work that remains</dt><dd>Planning and provenance bookkeeping at each demand</dd></dl></article></li><li class="rk-pipeline-step"><article><h3>Compile a closed state graph</h3><dl><dt>Public surface</dt><dd><code>prepare_reactive, ReactiveProgram, CompiledReactiveState</code></dd><dt>Result</dt><dd>Typed slots and generated getters</dd><dt>Runtime work that remains</dt><dd>Validity checks, required recipe calls, and invalidation worklists</dd></dl></article></li><li class="rk-pipeline-step"><article><h3>Compile a free state transition</h3><dl><dt>Public surface</dt><dd><code>compile_state_transition, initial_transition_state, partial</code></dd><dt>Result</dt><dd>CompiledStateTransition</dd><dt>Runtime work that remains</dt><dd>Authored writes, statically unrolled control, and demand-driven derived-field repairs</dd></dl></article></li><li class="rk-pipeline-step"><article><h3>Compile a bounded stateful method</h3><dl><dt>Public surface</dt><dd><code>compile_stateful, stateful_compiler_bindings, functionalize_stateful, stateful_snapshot</code></dd><dt>Result</dt><dd>Typed native state plus a backend-neutral functional transition</dd><dt>Runtime work that remains</dt><dd>Source-ordered structured control, canonical nested-state repairs, typed effects, and explicit overflow</dd></dl></article></li><li class="rk-pipeline-step"><article><h3>Inspect the selected graph</h3><dl><dt>Public surface</dt><dd><code>visualize, dot_source, save_visualization</code></dd><dt>Result</dt><dd>Plan/DAG view</dd><dt>Runtime work that remains</dt><dd>No effect on compilation</dd></dl></article></li></ol><p>Method-bearing compilation has a finite exported contract rather than a general Julia <code>prepare</code> API. <code>compile_state_transition</code> combines a stateless endpoint <code>KernelSpec</code> with a bound free update method. <code>compile_stateful</code>, <code>stateful_compiler_bindings</code>, and <code>functionalize_stateful</code> compile a captured stateful object and one selected method through explicit typed callable, nested-state, loop-bound, and argument-domain contracts. NUTS, log-density, and PPL artifacts are external compilation examples and acceptance evidence, not domain APIs owned by ReactiveKernels. The NUTS runtime and domain surface live in the <code>ReactiveKernelsNUTSExamples</code> nested package; a bare <code>using ReactiveKernels</code> does not load or export them. See <a href="/ReactiveKernels.jl/dev/compiler#what-the-nuts-proof-does-and-does-not-establish">What the NUTS proof does and does not establish</a>.</p><h2 id="The-stateless-compiler" tabindex="-1">The stateless compiler <a class="header-anchor" href="#The-stateless-compiler" aria-label="Permalink to &quot;The stateless compiler {#The-stateless-compiler}&quot;">​</a></h2><h3 id="1.-Graph-construction" tabindex="-1">1. Graph construction <a class="header-anchor" href="#1.-Graph-construction" aria-label="Permalink to &quot;1. Graph construction {#1.-Graph-construction}&quot;">​</a></h3><p>A <code>Value</code> has a stable process-unique identity and a declared Julia value type. Its name is only a diagnostic hint. Two values with the same name are distinct; generated local variables therefore never use a name as identity.</p><p>A <code>Recipe</code> records ordered input and output values, a callable operation, a finite non-negative planning cost, an optional structural-CSE key, and an effectful flag. Graph construction records producers but executes nothing. A <code>KernelSpec</code> adds named ports, a default HAVE/WANT boundary, and an optional ordinary call-signature adapter; it does not introduce another planner or runtime.</p><p>Stateless <code>@kernel</code> authoring supports:</p><ul><li><p>short or long function-shaped definitions;</p></li><li><p>required arguments, trailing positional defaults, and fixed keyword arguments, including required keywords;</p></li><li><p>defaults evaluated left to right at call time, with references to earlier arguments;</p></li><li><p>forward declarations and forward producer references;</p></li><li><p>one assignment per recipe, tuple assignment for a multi-output recipe, and repeated assignment for alternative producers;</p></li><li><p>an explicit return boundary or implicit derived sink outputs;</p></li><li><p>bare same-type identities, which become canonical aliases rather than copy recipes;</p></li><li><p>recipe costs and opt-in structural-CSE keys; and</p></li><li><p>arbitrary ordinary Julia inside a recipe right-hand side, including local control flow, exceptions, comprehensions, closures, and <code>do</code> blocks.</p></li></ul><p>The last item works because the recipe body is one opaque callable. The graph knows its free-port inputs and declared outputs, but it does not infer the callable&#39;s internal effects, branches, allocations, or mathematics.</p><p>Stateless signatures do not support positional or keyword splats. A port may be untyped metadata, but a typed port is checked at the generated function boundary. A defaulted or keyword argument remains a real HAVE port after the signature adapter has supplied its value.</p><h3 id="2.-Exact-havewant-planning" tabindex="-1">2. Exact have→want planning <a class="header-anchor" href="#2.-Exact-havewant-planning" aria-label="Permalink to &quot;2. Exact have→want planning {#2.-Exact-havewant-planning}&quot;">​</a></h3><p>Planning treats HAVE values as authoritative cut points. Even if a HAVE value also has producers, it is never recomputed. Repeated or structurally aliased HAVE entries collapse to one canonical input while the first-seen positional order is retained.</p><p>The planner then performs these steps:</p><ul><li><p><strong>Candidate frontier.</strong> Starting from every wanted value not already in HAVE, walk producers backward until reaching HAVE. This forms the candidate-recipe frontier. Effectful recipes are excluded.</p></li><li><p><strong>Exact subset search.</strong> Search exact subsets of that frontier with branch-and-bound. A search state contains selected recipes. Its available set is HAVE plus every selected output; its unresolved frontier is the unsatisfied WANTS plus inputs of selected recipes that are not yet available.</p></li><li><p><strong>Branching.</strong> Pick the first unresolved value and branch over every candidate recipe that can produce it. Non-negative costs allow pruning any branch already more expensive than the incumbent.</p></li><li><p><strong>Ranking.</strong> Rank complete selections lexicographically by total declared cost and then by number of recipes. Recipe identifiers provide deterministic traversal order; the cost is an author-supplied planning weight, not a timing estimate.</p></li><li><p><strong>Execution check.</strong> Accept a complete selection only if availability-based Kahn ordering can execute it. Each ready recipe adds all of its outputs to the available set. This avoids inventing a cycle when overlapping multi-output recipes admit a valid order through a different producer.</p></li><li><p><strong>Recorded result.</strong> Record the topological recipe order and one selected owner recipe for each produced canonical value. Collateral outputs from a non-owner recipe may be computed, but they never overwrite HAVE or an earlier selected owner.</p></li></ul><p>The result is exact for the implemented domain: a finite, acyclic graph with additive finite non-negative recipe costs. Search is exponential in the worst case and is intended for modest kernel graphs, not large-scale mathematical optimization. Cycles, impossible wants, and an effectful-only route fail with a <code>PlanningError</code>; impossible-query diagnostics report the missing producer frontier.</p><p>The planner does not do algebraic simplification, constant folding, common subexpression discovery from Julia code, shape inference, symbolic differentiation, or runtime profiling.</p><h3 id="3.-Structural-common-subexpression-elimination" tabindex="-1">3. Structural common-subexpression elimination <a class="header-anchor" href="#3.-Structural-common-subexpression-elimination" aria-label="Permalink to &quot;3. Structural common-subexpression elimination {#3.-Structural-common-subexpression-elimination}&quot;">​</a></h3><p>Structural CSE is explicit and conservative. A new recipe coalesces with an earlier recipe only when both are non-effectful, both have the same non-null CSE key, their canonical input identities match in order, and their output arities and corresponding declared types match. The new outputs become aliases of the old outputs.</p><p>The graph validates the complete alias mapping before mutation, including conflicting and cyclic multi-output mappings. Composition replays recipes through the same CSE rules, so equivalent fragments may coalesce.</p><p>A matching function name, equal output name, syntactically similar expression, or coincidentally equal runtime value is not CSE evidence. Effectful recipes are never CSE candidates. CSE is not an algebra system: commutativity, associativity, distributivity, and floating-point reassociation are never assumed.</p><h3 id="4.-Lowering-and-emitted-ABI" tabindex="-1">4. Lowering and emitted ABI <a class="header-anchor" href="#4.-Lowering-and-emitted-ABI" aria-label="Permalink to &quot;4. Lowering and emitted ABI {#4.-Lowering-and-emitted-ABI}&quot;">​</a></h3><p>Lowering turns the selected plan into a straight-line anonymous Julia function. It creates collision-free locals from canonical value identities, takes the selected operations in a positional tuple, and calls each operation through a literal tuple index. HAVE values appear in their preserved boundary order; WANT values appear in their requested return order. A single WANT returns one value and multiple WANTS return a tuple.</p><p>Multi-output recipes use one tuple destructure. If a later recipe emits a value already owned by HAVE or an earlier selected producer, lowering binds that collateral result to a discard local rather than changing the logical value. The generated function closes over no graph object and does not resolve operations through module globals.</p><p><code>RuntimeGeneratedFunctions</code> compiles the expression. The resulting <code>PreparedKernel</code> stores the compiled callable, its concrete operation tuple, and inspection metadata. Calling it performs no graph traversal, producer selection, topological sort, cache lookup, or dynamic scheduling. Julia still specializes and executes the recipe callables normally; their own dispatch, allocations, exceptions, and side effects are not erased by the graph compiler.</p><p><code>transform</code> applies user-supplied expression passes between lowering and compilation. The compiler preserves pass order but cannot prove that a pass preserves semantics. A transformed expression is therefore part of the user&#39;s trusted compilation boundary.</p><p><code>PreparationCache</code> is keyed by graph identity and version, canonical ordered HAVE/WANT signatures, and pass identities. Mutating a graph increments its version, so a cache entry for the old graph cannot be returned. Cache lookup is preparation-time work only.</p><h3 id="5.-Composition" tabindex="-1">5. Composition <a class="header-anchor" href="#5.-Composition" aria-label="Permalink to &quot;5. Composition {#5.-Composition}&quot;">​</a></h3><p>Low-level <code>compose</code> preserves globally stable <code>Value</code> identities when graph fragments already share them. Named <code>KernelSpec</code> composition instead creates a fresh graph, unifies same-name ports only when their declared types agree, copies both recipe sets and aliases, and reruns structural CSE. Its default call boundary remains the base spec&#39;s boundary; adopting the fragment boundary must be explicit.</p><p>Composition does not resolve same-name type disagreements, rename ports, infer adapter recipes, or merge incompatible call signatures semantically.</p><p>A direct stateless <code>KernelSpec</code> call in another stateless <code>@kernel</code> is the call-site-oriented composition form. Each call clones the nested graph with fresh value identities and aliases its typed default HAVE/WANT boundary to the arguments and assignment outputs. This permits repeated calls without internal name collisions and leaves no residual runtime call: planning, CSE, lowering, reactive preparation, visualization, batching, and AD all operate on the one fused graph.</p><h2 id="Batch-and-replica-lowering" tabindex="-1">Batch and replica lowering <a class="header-anchor" href="#Batch-and-replica-lowering" aria-label="Permalink to &quot;Batch and replica lowering {#Batch-and-replica-lowering}&quot;">​</a></h2><h3 id="Plate:-fuse-a-scalar-density-across-observations" tabindex="-1">Plate: fuse a scalar density across observations <a class="header-anchor" href="#Plate:-fuse-a-scalar-density-across-observations" aria-label="Permalink to &quot;Plate: fuse a scalar density across observations {#Plate:-fuse-a-scalar-density-across-observations}&quot;">​</a></h3><p><code>plate</code> starts from one scalar plan. It marks nominated HAVE ports as batched, then propagates batch-dependence through selected recipes in topological order. Recipes with no transitive dependency on a batched port are emitted once above the loop. Dependent recipes are emitted once per index. The single per-element WANT is either accumulated with a reducer (sum by default) or collected into a new vector.</p><p>The native reducing body materializes no per-observation result vector. A second eager tensor body expresses dependent operations as broadcasts and the final reduction as a tensor operation; an optional array-compiler extension selects it for traced arrays while ordinary Julia arrays retain the native loop.</p><p>The current plate domain is deliberately narrow:</p><ul><li><p>at least one named HAVE port must be batched;</p></li><li><p>every selected recipe must have exactly one output;</p></li><li><p>there must be exactly one scalar WANT, and it must transitively depend on a batched port;</p></li><li><p>all batched ports are indexed over the first nominated port&#39;s <code>eachindex</code>, so compatible axes are the caller&#39;s responsibility; and</p></li><li><p>a non-null reducer is spliced as a bare callable, with the documented surface intended for Base reducer symbols.</p></li></ul><p>This is map-plus-reduce/collect with invariant hoisting. It is not a general scan, fold with loop-carried state, segmented reduction, parallel reduction, associative tree reduction, or automatic prepared-kernel batching transform.</p><h3 id="Prepared-kernel-composition-is-flattened" tabindex="-1">Prepared-kernel composition is flattened <a class="header-anchor" href="#Prepared-kernel-composition-is-flattened" aria-label="Permalink to &quot;Prepared-kernel composition is flattened {#Prepared-kernel-composition-is-flattened}&quot;">​</a></h3><p>If an outer <code>@kernel</code> calls a prepared RK kernel, <code>prepare</code> treats it as compiler-owned recipe code: it alpha-renames and splices the inner generated statements into the outer function, remaps its operation-table slots, and emits one flat executable operation table. The inner <code>PreparedKernel</code>, <code>Plan</code>, and generated <code>Expr</code> are not runtime operations.</p><p>For example, an author can prepare <code>reduced = plate(scalar_density; have = (:x, :μ, :logσ), want = :ld, batched = :x)</code> and call <code>reduced(x, μ, logσ)</code> inside an outer <code>@kernel</code>. The outer prepared function contains the plated statements directly rather than a runtime call through <code>reduced</code>.</p><p>Nested plates retain both compiler products: ordinary arrays select the native fused loop, while traced arrays select the tensorized broadcast/reduce body. This is the static-friendly density boundary used by the PPL examples: ordinary Julia consumers see one flat generated function, and Reactant still sees the array-native plate form.</p><h3 id="Replica:-lift-the-entire-scalar-callable" tabindex="-1">Replica: lift the entire scalar callable <a class="header-anchor" href="#Replica:-lift-the-entire-scalar-callable" aria-label="Permalink to &quot;Replica: lift the entire scalar callable {#Replica:-lift-the-entire-scalar-callable}&quot;">​</a></h3><p><code>replica</code> keeps a complete scalar <code>PreparedKernel</code> as the mathematical source of truth and maps it over one trailing replica axis on selected HAVE ports. Scalar batched ports become vectors, array ports gain one final dimension, and outputs are stacked along the same final dimension. Reductions inside the scalar kernel keep their original dimensions; they do not accidentally reduce across replicas.</p><p>Selected input and output types must be numbers or arrays. Native execution validates the extra rank and equal replica counts, evaluates scalar replicas, copies array slices, and stacks results, so it is a semantic transform rather than a native zero-allocation promise. Optional array-compiler integration may lower the same mapping as a backend batch primitive.</p><h2 id="Incremental-and-compiled-reactive-execution" tabindex="-1">Incremental and compiled reactive execution <a class="header-anchor" href="#Incremental-and-compiled-reactive-execution" aria-label="Permalink to &quot;Incremental and compiled reactive execution {#Incremental-and-compiled-reactive-execution}&quot;">​</a></h2><h3 id="Open-ended-ReactiveState" tabindex="-1">Open-ended <code>ReactiveState</code> <a class="header-anchor" href="#Open-ended-ReactiveState" aria-label="Permalink to &quot;Open-ended `ReactiveState` {#Open-ended-ReactiveState}&quot;">​</a></h3><p><code>ReactiveState</code> is orchestration above the stateless planner. Each stored value has a monotonic version, a policy (<code>source</code>, <code>reactive</code>, or <code>frozen</code>), and—for a reactive materialization—the versions of the actual selected HAVE leaves that produced it.</p><p>On a demand, the state performs these operations in dependency order:</p><ul><li><p><strong>Effective HAVE.</strong> Form effective HAVE from sources, frozen cut points, and recursively provenance-valid materializations;</p></li><li><p><strong>Initial plan.</strong> Plan the missing request;</p></li><li><p><strong>Boundary extension.</strong> Extend the request only with nominated materialization boundaries that this plan actually produces;</p></li><li><p><strong>Prepared execution.</strong> Replan the extended boundary, reuse or prepare its stateless kernel, and call it with only the HAVE leaves the plan consumes; and</p></li><li><p><strong>Stored result.</strong> Store nominated results with provenance from the selected producer paths, not from unused alternatives.</p></li></ul><p>Validation uses a recursion-stack cycle check, so shared provenance subgraphs are valid while true recursive provenance is not. Sources and frozen values stop validation and remain authoritative. A checkpoint refuses an absent or stale value.</p><p>This mode supports arbitrary later WANTS but deliberately performs dictionary, planning, provenance, and cache work at the demand boundary. Only the prepared kernel call inside it has the stateless hot-path guarantee.</p><h3 id="Closed-ReactiveProgram" tabindex="-1">Closed <code>ReactiveProgram</code> <a class="header-anchor" href="#Closed-ReactiveProgram" aria-label="Permalink to &quot;Closed `ReactiveProgram` {#Closed-ReactiveProgram}&quot;">​</a></h3><p><code>prepare_reactive</code> selects one fixed HAVE/WANT plan once. It assigns each reachable canonical value a typed <code>Ref</code> slot and a <code>ReactiveValue</code> whose slot index is a type parameter. It precomputes direct dependents and compiles one lazy getter per reachable value.</p><p>A getter checks the target validity bit. If stale, it recursively ensures the selected recipe&#39;s inputs, calls that recipe once, stores every owned output, and marks the stored outputs valid. A source with no producer must already be valid. Setting or touching a HAVE slot walks the precomputed dependent closure with a reusable stack and clears validity bits, stopping at frozen derived cut points.</p><p>The plan, tuple layout, recipe selection, slot types, and getter expressions are shared by all program instances. Runtime state is only slots, valid/frozen bit vectors, and invalidation scratch. Mutating the source graph after preparation causes an actionable version error rather than silently using a stale program.</p><p><code>set!</code>, <code>touch!</code>, and public <code>mutate!</code> are restricted to the declared HAVE boundary. <code>mutate!</code> invalidates in a <code>finally</code> block because a throwing mutation may already have changed its object. Derived values may be frozen as explicit cut points and checkpointed into a later instance. State copies duplicate array storage; <code>copy_group!</code> copies selected HAVE slots left to right and is order-dependent when source and destination groups overlap.</p><p>A compiled state is mutable and not thread-safe. A generated getter can be inferred and allocation-free for a compatible operation set, but the compiler does not promise that arbitrary recipe operations allocate nothing.</p><h2 id="Allocation-reusing-preparation" tabindex="-1">Allocation-reusing preparation <a class="header-anchor" href="#Allocation-reusing-preparation" aria-label="Permalink to &quot;Allocation-reusing preparation {#Allocation-reusing-preparation}&quot;">​</a></h2><p>The optional MutatingFunctions extension changes call lowering, not graph semantics or producer selection.</p><p><code>prepare_nonallocating</code> requires every selected recipe to have one output. It gives each recipe a persistent typed cache. The first invocation seeds the cache; later invocations offer it to <code>MutatingFunctions.apply!!</code>. The generic fallback may still allocate, so zero allocation is a property of the complete selected operation set and runtime types, never a planner theorem.</p><p>The cached result is borrowed. An aliasing operation may retain caller-owned input storage, and the next call may overwrite the prior returned object. A <code>NonAllocatingKernel</code> instance is not reentrant, thread-safe, or suitable for concurrent callers.</p><p><code>prepare_reactive_nonallocating</code> instead uses each compiled-state output slot as the per-instance cache for selected mutable single-output recipes. Invalidation clears validity without clearing storage, allowing a later recomputation to reuse the buffer. Multi-output and immutable-output recipes remain on the pure path. Independent state instances own independent buffers, but a returned mutable value is still borrowed from its state.</p><h2 id="Source-captured-method-compiler" tabindex="-1">Source-captured method compiler <a class="header-anchor" href="#Source-captured-method-compiler" aria-label="Permalink to &quot;Source-captured method compiler {#Source-captured-method-compiler}&quot;">​</a></h2><p>Method-bearing <code>@kernel</code> definitions enter a stricter compiler. They are not opaque recipe closures and are not analyzed through Julia&#39;s compiled IR.</p><h3 id="Authoritative-input-is-captured-source" tabindex="-1">Authoritative input is captured source <a class="header-anchor" href="#Authoritative-input-is-captured-source" aria-label="Permalink to &quot;Authoritative input is captured source {#Authoritative-input-is-captured-source}&quot;">​</a></h3><p>At definition time, the macro stores detached, structurally frozen source ASTs, definition-module identity, port metadata, and exact callee-registration snapshots. Later analysis walks that stored source. It does not call <code>code_lowered</code>, <code>code_typed</code>, <code>CodeInfo</code>, <code>Core.Compiler</code>, method inference, or arbitrary function-body inspection. Rebinding an authored callee slot to a different identity is detected and rejected.</p><p>This boundary makes the compiler&#39;s claim finite: it compiles the language it captured and the effects explicitly registered for it. It makes no claim to understand arbitrary Julia hidden behind an unregistered call.</p><h3 id="Two-stateful-authoring-modes" tabindex="-1">Two stateful authoring modes <a class="header-anchor" href="#Two-stateful-authoring-modes" aria-label="Permalink to &quot;Two stateful authoring modes {#Two-stateful-authoring-modes}&quot;">​</a></h3><p>A body containing nested methods is an object kernel. Its receiver is implicit: bare unshadowed port names are owner fields, while formals and locals shadow fields lexically. Nested methods declare no <code>self</code> or <code>__self__</code> formal. The token <code>__self__</code> is legal only as the first positional actual of a sibling method call; using it as a field qualifier or ordinary value is rejected.</p><p>A methodless definition that mutates a field of its first positional argument, or whose name ends in <code>!!</code>, is a free update method. Its first positional argument is the explicit subject. The <code>!!</code> spelling is an explicit strong same-object update contract, not a heuristic about a hidden implementation.</p><p>Everything else remains the stateless <code>KernelSpec</code> path. These modes are selected from syntax before any recipe runs.</p><h3 id="Normalized-MethodIR" tabindex="-1">Normalized <code>MethodIR</code> <a class="header-anchor" href="#Normalized-MethodIR" aria-label="Permalink to &quot;Normalized `MethodIR` {#Normalized-MethodIR}&quot;">​</a></h3><p>Captured methods normalize into immutable <code>MethodIR</code> values. The IR represents:</p><ul><li><p>literals, locals, formals, owner-field paths, indexes, property access, tuples, named tuples, short-circuit values, and conditional values;</p></li><li><p>local assignment, direct/indexed/nested place writes, place swaps, and write-then-return;</p></li><li><p>branches, guards, <code>for</code> and <code>while</code> loops, <code>break</code>, <code>continue</code>, and returns;</p></li><li><p>sibling calls, subject-method calls, registered callable-field calls, registered primitive/intrinsic calls, and unresolved external calls; and</p></li><li><p>positional/keyword formals, defaults, overload candidates, source-order reads, branch groups, and loop-carried access events.</p></li></ul><p>Each method records three independent facts: control shape (straight, branch, loop, or recursive), local effects, and unresolved dependencies. Structured access events keep branch alternatives separate and mark loops that may execute zero times. Every call records its actual-value reads. This prevents a flat source scan from inventing definite assignment or hiding a read behind a branch.</p><p>Normalization rejects malformed or unsoundly ambiguous source, including a computed/dynamic callee, a positional splat at a call site, an unresolved qualified callee, a maybe-bound local read, unsupported tuple-place mutation, and a mutation whose owner root cannot be represented. An unregistered external may be represented as opaque for diagnostics, but executable ownership/effect lowering must resolve it or reject it.</p><h3 id="Effects,-ownership,-and-physical-storage" tabindex="-1">Effects, ownership, and physical storage <a class="header-anchor" href="#Effects,-ownership,-and-physical-storage" aria-label="Permalink to &quot;Effects, ownership, and physical storage {#Effects,-ownership,-and-physical-storage}&quot;">​</a></h3><p>Calls are authorized by exact identity, not by spelling. Built-in compiler descriptors describe arity, reads, writes, result aliasing/borrowing, and RNG position. The former <code>@rk_pure</code>, <code>@rk_borrows</code>, and <code>@rk_rng</code> declarations have been removed. External fixtures use ordinary visible arithmetic/control or a captured sibling <code>@kernel</code> method. Undeclared ordinary helpers remain unsupported and are not made pure because they have an operator-like name or receive an argument named <code>rng</code>. <code>@node</code> is unrelated to this removal boundary and remains part of graph authoring.</p><p>For compiler-known primitives, the implementation also checks the concrete specialization domain. Sanctioned Base numeric scalars, dense arrays, and the specific linear-algebra wrappers required by the compiled kernels are accepted. A user subtype that dispatches a custom overload through the same generic function is rejected unless it is admitted by an exact, validated compiler rule. This prevents generic-function identity from smuggling unknown effects into compiled code.</p><p>The factory computes an interprocedural ownership fixed point over direct writes, sibling calls, callable fields, borrowed aliases, and strong-copy operations. It separates producer-owned endpoint storage from shared read-authority. Its immutable plan records canonical slots, role and physical index, authoritative HAVE, selected producer recipes, dependents, entry-current bits, captured operation handles, and recipe input/output ownership.</p><p>Storage is concrete tuples/structs and validity masks; author symbols are only labels. Copied child endpoints get distinct owned storage but may share one read-only authority object. Two paths using the same graph value identity remain distinct because schedule state is keyed by physical owner/view path plus canonical value identity.</p><h3 id="Currentness-and-exception-semantics" tabindex="-1">Currentness and exception semantics <a class="header-anchor" href="#Currentness-and-exception-semantics" aria-label="Permalink to &quot;Currentness and exception semantics {#Currentness-and-exception-semantics}&quot;">​</a></h3><p>A direct write makes its target current and kills the transitive dependents of that target in the selected producer graph. Before a derived read, lowering recursively ensures the selected producer&#39;s inputs and executes that producer once if needed. One recipe execution produces its selected owned output set atomically; collateral outputs owned by another recipe are not blessed.</p><p>A method may directly write only an authoritative source slot. Treating one value as both directly written and plan-produced is rejected. A stale derived read with no selected producer is also rejected. Entry currentness is an explicit factory contract, never inferred by assuming that all derived values are valid.</p><p>Validity changes are exception-safe but values are not transactionally rolled back. Dependents are invalidated before or with a mutation. A throwing recipe does not bless its outputs. If several writes have already executed before a later throw, their invalidations remain, so a retry recomputes from the actual partially mutated state rather than reading stale cached values.</p><h3 id="Control-lowering" tabindex="-1">Control lowering <a class="header-anchor" href="#Control-lowering" aria-label="Permalink to &quot;Control lowering {#Control-lowering}&quot;">​</a></h3><p>The general control compiler distinguishes acyclic work from recursive or suspending work. Acyclic sibling methods and their early returns are inlined into the caller&#39;s continuation. Non-suspending loops remain native Julia loops. Recursive strongly connected components and callers that suspend across them are defunctionalized into finite method/PC dispatch, concrete frame stores, and an isbits control stack. Values live across a suspension are stored explicitly; the generated dispatcher contains no boxed closure or runtime method table.</p><p>The implemented generic control surface is narrower than Julia. In the PC machine, a suspending <code>for</code> loop currently requires a two-bound unit range; non-suspending loops can retain an arbitrary native iterable. Exact overload narrowing is required before emission. Unsupported statements or unresolved effects reject instead of falling back to interpretation.</p><h3 id="Functional-free-state-transitions" tabindex="-1">Functional free state transitions <a class="header-anchor" href="#Functional-free-state-transitions" aria-label="Permalink to &quot;Functional free state transitions {#Functional-free-state-transitions}&quot;">​</a></h3><p><code>compile_state_transition(spec, transition, endpoint_args; endpoint_kwargs=(;))</code> is the public, backend-neutral subset of the method-bearing compiler. <code>spec</code> supplies the endpoint&#39;s authoritative sources and have→want recipes. <code>transition</code> is a registered free mutating <code>@kernel</code>, or a <code>partial</code> of one that binds all of its required keyword controls. Construction uses the original endpoint and transition signature binders; it does not match the kernel&#39;s name, field names, or a program-counter census.</p><p>The result is a <code>CompiledStateTransition</code>. <code>initial_transition_state(result)</code> returns an isolated, fully materialized named state while preserving external callable authorities by identity. Calling the result with that state is functional: an authored array write produces a new array value, invalidates the written source&#39;s transitive derived closure, and recomputes a stale derived field only at its next source-ordered read. The same generated program is ordinary Julia and static program metadata to optional array compilers such as Reactant. Every call validates the exact recursive state layout, owned-array axes, canonical aliases, and external authority identities captured at construction. Group-aware structural copies isolate owned arrays without invoking user <code>deepcopy</code> methods or duplicating callable authorities. Authored broadcast assignment between builtin floating types retains Julia&#39;s destination-element conversion, including Float64 intermediates written into Float32 storage.</p><p>The initial public source subset is intentionally finite. It admits direct owned-field writes, exact captured <code>map(copy, tuple)</code>, tuple and named destructuring, bound non-Boolean numeric controls, and integer <code>Base.Colon</code> loops whose bounds are entirely static. Static loops are unrolled during lowering so validity is propagated through every authored iteration; this is not a host loop around traced execution. Indexed destinations, data-dependent branches/loops, arbitrary higher-order calls, and opaque callbacks reject. Callback computations used by endpoint recipes must be explicit endpoint ports so their authority is auditable and their identity is preserved.</p><p>The six ReactiveHMC phase-point examples exercise this one compiler path for Gaussian and relativistic Euclidean, Riemannian, and diagonal-SoftAbs geometry. Both generalized leapfrog and implicit midpoint remain their ordinary authored mathematical kernels. Those examples validate a reusable compiler capability; they do not add geometry or integrator cases to compiler code.</p><h3 id="Functional-stateful-methods-and-nested-state-contracts" tabindex="-1">Functional stateful methods and nested state contracts <a class="header-anchor" href="#Functional-stateful-methods-and-nested-state-contracts" aria-label="Permalink to &quot;Functional stateful methods and nested state contracts {#Functional-stateful-methods-and-nested-state-contracts}&quot;">​</a></h3><p><code>compile_stateful</code> prepares a method-bearing <code>@kernel</code> after every callable field has an explicit entry in <code>stateful_compiler_bindings</code>. A <code>pure_callable_port</code> declares one exact argument/result contract and no mutation. An <code>effect_callable_port</code> additionally declares which positional values may be replaced, a functional lowering, and an auxiliary effect value. Both validate their replacement arguments, logical result, and effect-state type after lowering. A control-dependent lowering must be wrapped in <code>total_functional_lowering</code>, explicitly asserting that it is pure and total over inactive predicated values; an arbitrary partial callback rejects. <code>effect_lowering_port</code> is the distinct contract for a non-executable authority token whose compiler lowering has independent source-oracle evidence. It does not mislabel that token as an ordinary source callback. <code>StatefulStateValue</code> is the contract marker for a callback whose authored argument is <code>__self__</code>; the compiler substitutes and validates the concrete whole-state NamedTuple type rather than asking an application to reconstruct private storage types.</p><p>Effect classification is currently an internal conservative compiler rule, not new public authoring syntax. A port that can replace any argument or return a meaningful value is causal: its state stays in the fixed compiled ABI, and host feedback can affect a later transition only after an explicit barrier and re-entry as a separately typed input. A source-backed port with no written arguments and <code>Nothing</code> result has no causal channel, so the compiler generates a split kernel automatically. The Reactant region emits source-order records into fixed-capacity slots with a logical activity mask and count plus sticky overflow; ordinary Julia then calls <code>drain_observations!(transition, result)</code> after the compiled call returns. No <code>push!</code>, callback invocation, allocation-driven shape growth, or implicit host round trip occurs inside the compiled invocation, and callers may schedule the drain on their own Julia task after that synchronization point.</p><p>A compiler-only observational authority still runs its reviewed total lowering to form one fixed-shape summary. That summary is returned in the same outbox; the existing <code>effects</code> result also mirrors it for fixed-shape compatibility, but every invocation starts from the declared initial value rather than reading the prior summary. Consequently observation history cannot become an implicit future compiled input. Outbox overflow rejects before partial host replay, and a record whose type or axes would change is diagnosed as forbidden outbox growth. Runtime reusable-call mismatches report both expected and observed type/axes signatures and instruct the caller to explicitly recompile; the host guard performs that rejection before entering the backend executable.</p><p><code>structured_state_port(compiled_transition)</code> binds a nested state field to the canonical alias groups, external authority identities, writable source fields, and derived-recipe closure of an ordinary <code>CompiledStateTransition</code>. A nested write is admitted only when that transition declares its first path field writable. The compiler updates every alias in the written canonical group and eagerly repairs the active transitive derived closure before the outer method continues. Structural copies and predicated rollback select or copy once per canonical group, so internal aliases remain aliases and external callbacks remain identical. None of this lowering dispatches on sampler, geometry, method, or field names.</p><h3 id="Prototype-derived-finite-structural-containers" tabindex="-1">Prototype-derived finite structural containers <a class="header-anchor" href="#Prototype-derived-finite-structural-containers" aria-label="Permalink to &quot;Prototype-derived finite structural containers {#Prototype-derived-finite-structural-containers}&quot;">​</a></h3><p>The functional state-machine compiler has a closed ABI for fixed-capacity structural vectors. Construction starts from the actual prototype values; it does not infer a generic container promise from <code>Vector{T}</code> alone. The contract freezes the element type and capacity, recursive named-tuple and tuple layout, <code>Diagonal</code> and <code>Cholesky</code> wrappers, array element types and axes, static authority identities, within-element owned-array aliases, and the requirement that owned storage from different elements remain disjoint.</p><p>Dynamic leaves are exactly <code>Bool</code>, the signed and unsigned 8-, 16-, 32-, and 64-bit integers, <code>Float16</code>, <code>Float32</code>, <code>Float64</code>, and dense arrays of those types. The backend representation is a structure of arrays: one typed numeric column per logical owned leaf group, with capacity as the last array dimension. Static authorities never enter that numeric ABI. Unsupported leaves, wrapper types, shape changes, merged aliases, separated required aliases, or shared owned storage across elements reject during contract validation.</p><p>The lowering has topology-aware read, write, copy, swap, and whole-container select operations. An active out-of-bounds index sets overflow and commits no partial write, copy, or swap. Raw input and output are both validated for exact column names and order, numeric types, axes, and capacity. Output validation happens before reconstruction, so restoring static identities or canonical aliases cannot hide a counterfeit backend result. This is a compiler-internal finite ABI, not a general exported container API and not permission for dynamic <code>push!</code>, <code>pop!</code>, resizing, or arbitrary element types.</p><p>Primitive emission is closed over exact captured builtins and concrete specializations. <code>Base.abs</code> is admitted here only for one builtin <code>AbstractFloat</code> scalar. <code>Base.div</code> requires two operands of the identical builtin non-<code>Bool</code> integer type. Integer <code>abs</code>, mixed-integer <code>div</code>, <code>Bool</code>, 128-bit integers, numeric wrappers, and user overloads do not widen those rules; they reject before executable lowering.</p><p><code>functionalize_stateful(kernel, Val(:method); max_iterations, argument_types)</code> produces the backend-neutral functional program; <code>stateful_snapshot</code> supplies its initial value surface. Dynamic structured control requires a positive finite loop bound. A bound violation, an active out-of-bounds indexed access, or exhausted effect storage sets a control overflow and rolls state plus auxiliary effects back atomically. The typed functional entry validates the outer plan&#39;s canonical aliases as well as any nested structured aliases. A fresh Reactant compilation rejects an independently converted counterfeit alias. For repeated calls, the raw backend executable must be wrapped with <code>validated_compiled_transition(compiled, transition)</code>. That generic host guard revalidates layout, shapes, canonical aliases, structured ports, and external authorities before dispatch. It restores frozen callable and nested structured external authorities (which are compiler metadata rather than backend data), then validates the returned owned-data alias topology. The effects-positional form also checks the dynamic effects carrier and method arguments. The backend executable itself enforces its traced leaf signature; the raw executable alone is outside the safe repeated-call contract because a flattened buffer ABI cannot represent Julia object identity. Straight-line and state-machine gates both prove valid repeated execution, identity-preserving outputs, and rejection of an independently converted counterfeit alias.</p><p>Dynamic unit ranges are bounded with guarded successor steps in the authored integer type. The compiler neither subtracts extreme bounds nor adds host-Int offsets, avoiding wraparound and small-integer promotion while preserving an explicit overflow/rollback result.</p><p>The typed <code>OrderedRNGReplay</code> value carries independent floating normal, Boolean uniform, and exact Float64 exponential tapes, per-kind cursors, one global ordered event tape/cursor, and sticky overflow. <code>randn!</code> is admitted only for an immediate whole-vector state replacement with the declared destination/result alias; <code>rand(rng, Bool)</code> retains the exact captured type descriptor. Conditional source paths consume only the effects they enter, and an event-kind mismatch or exhausted tape fails closed while preserving the attempted replay receipt. Every consumed normal column is rechecked for finiteness and every consumed exponential for finiteness and nonnegativity, including dynamic traced tapes; invalid consumed data sets sticky overflow and atomically rolls state back, while unconsumed padding remains irrelevant.</p><p>Causal auxiliary effects are explicit continuation state, not hidden compiler state. <code>initial_transition_effects(transition)</code> constructs the first value. Ordinary Julia may pass it as the <code>effects</code> keyword; <code>transition_with_effects(transition)</code> provides a positional callable for optional compilers, so callers can feed returned causal effects into the next compiled invocation. Compiler-only observational summaries may remain in that result carrier for compatibility, but are reset rather than consumed on the next invocation and are authoritatively exposed by <code>drain_observations!</code>. Compiler program metadata is recursively snapshotted from a finite immutable/builtin domain; later mutation of authoring configuration cannot change compiled semantics.</p><p>The fixed-step ReactiveHMC fixture is the first combined acceptance case. Its unchanged mathematical source refreshes momentum, executes generalized leapfrog, records statistics before divergence exit, conditionally consumes an exponential draw, and copies an accepted endpoint. The statistics callback has no Reactant implementation: the generic compiler infers it is observational, returns its bounded records, and the Julia drain replays the collector after the barrier. The same generic compiler path matches the independent accept, reject, and divergence receipt in ordinary Julia and Reactant. A separate pinned physical case locks Float32 position/momentum/normal draws together with the authored Float64 <code>randexp</code> result, exact bits, destination alias, and global RNG event order. That evidence admits reusable nested-state currentness, typed effects, ordered RNG, and mixed floating assignment capabilities; it is not an HMC case in compiler code.</p><p>The mixed-precision gate separates source semantics from backend floating rounding. Raw tape bits, tape and destination types, the <code>randn!</code> result alias, event order, cursors, shapes, overflow/rollback, and every control decision are exact. Ordinary Julia execution of the generic lowering is also bit-identical to the independent physical receipt. The backend-neutral emitter preserves the authored operator tree: it introduces no <code>@fastmath</code>, <code>muladd</code>, <code>fma</code>, or arithmetic reassociation, and contains no HMC/field-name case. Reactant keeps the exact Float32/Float64 field types and is checked against the physical result with explicit ULP distances, not an <code>isapprox</code> tolerance: positions <code>[0, 0]</code>; initial/final momenta <code>[1, 0]</code> Float32 ULPs; initial/final Hamiltonians <code>67_108_864</code> Float64 ULPs; the three energy diagnostics <code>[68_719_476_736, 0, 8_589_934_592]</code> Float64 ULPs; and final <code>dham</code> <code>8_589_934_592</code> Float64 ULPs. The apparently large Float64 counts reflect diagnostics stored as Float64 after cancellation along a Float32 trajectory; they are the exact pinned distances, not a broad error allowance. Its divergence and acceptance margins remain greater than <code>900</code> and <code>0.49</code>, respectively, so the recorded rounding cannot approach a control threshold. A widened Float64 normal tape is rejected by both native and Reactant entry contracts.</p><h2 id="What-the-NUTS-proof-does-and-does-not-establish" tabindex="-1">What the NUTS proof does and does not establish <a class="header-anchor" href="#What-the-NUTS-proof-does-and-does-not-establish" aria-label="Permalink to &quot;What the NUTS proof does and does not establish {#What-the-NUTS-proof-does-and-does-not-establish}&quot;">​</a></h2><p>The source-captured compiler&#39;s strongest end-to-end external exemplar is the reviewed eight-spec NUTS fixture shown on the <a href="./nuts">NUTS sampling</a> page. It exercises alternative recipe planning, destination-bound potential/gradient production, owned and shared storage, endpoint copies, mutation kills, recomputation, recursive tree control, loops, early returns, RNG effects, optional statistics, dual averaging, and Welford accumulation.</p><p>The sealed native path cold-compiles detached <code>MethodIR</code> into a registry-free type tree. Callable values live in one concrete tuple indexed from type metadata. The native encoder admits exactly the source forms needed by that fixture and rejects anything else, including opaque calls, unresolved overload sets, call-site keyword splats, formal splats, typed native-NUTS formals, chained comparisons, raw <code>@node</code> payloads in control metadata, unsupported structural literals, and subject-method calls inside the NUTS object methods.</p><p>Construction produces fixed-depth concrete endpoint/tree/proposal storage and a certificate binding owner token, root token, immutable plan type/key, native program type, control fingerprint, physical roles, selected recipes, emitted operations, and frame/root/scratch types. The acceptance evidence for that artifact verifies same-object mutation, inferred concrete return, exact zero allocation after construction, Float32/Float64 paths, two RNG types, exception/currentness behavior, and an instrumented emitted-operation census. The sealed acceptance root consults no graph, planner, mutable registry, or later-world method lookup.</p><p>That proof is deliberately <strong>not</strong> a general Julia-recursion compiler proof. It establishes the captured fixture and the encoder&#39;s rejection boundary. It does not establish arbitrary recursion, arbitrary containers or numeric subtypes, arbitrary integrators, PPL semantics, or a stable exported constructor for every method-bearing <code>@kernel</code>.</p><p>There is also a package-boundary distinction. The sealed <code>_build_nuts_sampler</code> compiler, fixture entry, certificate accessors, and the compiled-reactive <code>nuts_state</code> / <code>CompiledNUTSState</code> comparison path all live in the explicitly loaded external exemplar. None is loaded or exported by <code>ReactiveKernels</code>.</p><p>Therefore “the sealed native compiler runs the NUTS fixture” must not be read as “ReactiveKernels exports or promises a NUTS API.” The two external paths are implemented and tested as compiler evidence, but neither turns NUTS, log-density, or PPL models into the package&#39;s domain contract.</p><h2 id="Definitive-support-matrix" tabindex="-1">Definitive support matrix <a class="header-anchor" href="#Definitive-support-matrix" aria-label="Permalink to &quot;Definitive support matrix {#Definitive-support-matrix}&quot;">​</a></h2>',122)),t[2]||(t[2]=e("section",{class:"rk-result-table-section"},[e("h3",null,"Definitive support matrix"),e("p",{class:"rk-result-table-note"},"Select a column heading to sort."),e("div",{class:"rk-sortable-table-wrap"},[e("table",{class:"rk-result-table htmxo-sortable-table",role:"grid"},[e("thead",null,[e("tr",null,[e("th",{onclick:"sortTable(0, this)",class:"u-pointer"},"Capability"),e("th",{onclick:"sortTable(1, this)",class:"u-pointer"},"Supported"),e("th",{onclick:"sortTable(2, this)",class:"u-pointer"},"Boundary or rejection")])]),e("tbody",{id:"compiler-capability-table"},[e("tr",null,[e("td",{"data-sort-value":"Exact selection among alternative producers"},"Exact selection among alternative producers"),e("td",{"data-sort-value":"Yes"},[e("span",{class:"rk-support rk-support-yes"},"Yes")]),e("td",{"data-sort-value":"Finite acyclic graph; additive non-negative declared costs; exponential worst case"},"Finite acyclic graph; additive non-negative declared costs; exponential worst case")]),e("tr",null,[e("td",{"data-sort-value":"Multi-output stateless recipes"},"Multi-output stateless recipes"),e("td",{"data-sort-value":"Yes"},[e("span",{class:"rk-support rk-support-yes"},"Yes")]),e("td",{"data-sort-value":"One call, ordered tuple outputs; selected-owner rules discard collateral duplicates"},"One call, ordered tuple outputs; selected-owner rules discard collateral duplicates")]),e("tr",null,[e("td",{"data-sort-value":"Arbitrary Julia inside a stateless recipe"},"Arbitrary Julia inside a stateless recipe"),e("td",{"data-sort-value":"Yes, as opaque execution"},[e("span",{class:"rk-support rk-support-yes"},"Yes, as opaque execution")]),e("td",{"data-sort-value":"No internal effect, algebra, allocation, or control-flow analysis"},"No internal effect, algebra, allocation, or control-flow analysis")]),e("tr",null,[e("td",{"data-sort-value":"Graph structural CSE"},"Graph structural CSE"),e("td",{"data-sort-value":"Yes, opt-in"},[e("span",{class:"rk-support rk-support-yes"},"Yes, opt-in")]),e("td",{"data-sort-value":"Same non-null key, canonical inputs, output arity/types, non-effectful only"},"Same non-null key, canonical inputs, output arity/types, non-effectful only")]),e("tr",null,[e("td",{"data-sort-value":"Algebraic/symbolic optimization"},"Algebraic/symbolic optimization"),e("td",{"data-sort-value":"No"},[e("span",{class:"rk-support rk-support-no"},"No")]),e("td",{"data-sort-value":"No commutativity, reassociation, constant folding, symbolic differentiation, or cost measurement"},"No commutativity, reassociation, constant folding, symbolic differentiation, or cost measurement")]),e("tr",null,[e("td",{"data-sort-value":"Straight-line native preparation"},"Straight-line native preparation"),e("td",{"data-sort-value":"Yes"},[e("span",{class:"rk-support rk-support-yes"},"Yes")]),e("td",{"data-sort-value":"Recipe operations still obey ordinary Julia behavior"},"Recipe operations still obey ordinary Julia behavior")]),e("tr",null,[e("td",{"data-sort-value":"User AST passes"},"User AST passes"),e("td",{"data-sort-value":"Yes"},[e("span",{class:"rk-support rk-support-yes"},"Yes")]),e("td",{"data-sort-value":"User is responsible for semantic preservation"},"User is responsible for semantic preservation")]),e("tr",null,[e("td",{"data-sort-value":"Multiple WANTS"},"Multiple WANTS"),e("td",{"data-sort-value":"Yes"},[e("span",{class:"rk-support rk-support-yes"},"Yes")]),e("td",{"data-sort-value":"Returned in request order"},"Returned in request order")]),e("tr",null,[e("td",{"data-sort-value":"Stateless signature defaults/keywords"},"Stateless signature defaults/keywords"),e("td",{"data-sort-value":"Yes"},[e("span",{class:"rk-support rk-support-yes"},"Yes")]),e("td",{"data-sort-value":"Fixed arguments; no positional/keyword splats"},"Fixed arguments; no positional/keyword splats")]),e("tr",null,[e("td",{"data-sort-value":"Plate map/reduce or collect"},"Plate map/reduce or collect"),e("td",{"data-sort-value":"Yes"},[e("span",{class:"rk-support rk-support-yes"},"Yes")]),e("td",{"data-sort-value":"Single-output recipes and one batch-dependent scalar WANT; no scan/fold/parallel tree reduction"},"Single-output recipes and one batch-dependent scalar WANT; no scan/fold/parallel tree reduction")]),e("tr",null,[e("td",{"data-sort-value":"Whole-kernel replica axis"},"Whole-kernel replica axis"),e("td",{"data-sort-value":"Yes"},[e("span",{class:"rk-support rk-support-yes"},"Yes")]),e("td",{"data-sort-value":"Number/array ports and outputs; native map/stack may allocate"},"Number/array ports and outputs; native map/stack may allocate")]),e("tr",null,[e("td",{"data-sort-value":"Open-ended reactive demands"},"Open-ended reactive demands"),e("td",{"data-sort-value":"Yes"},[e("span",{class:"rk-support rk-support-yes"},"Yes")]),e("td",{"data-sort-value":"Planning/provenance work remains at each demand"},"Planning/provenance work remains at each demand")]),e("tr",null,[e("td",{"data-sort-value":"Fixed compiled reactive state"},"Fixed compiled reactive state"),e("td",{"data-sort-value":"Yes"},[e("span",{class:"rk-support rk-support-yes"},"Yes")]),e("td",{"data-sort-value":"Graph and boundary frozen; mutable instance is not thread-safe"},"Graph and boundary frozen; mutable instance is not thread-safe")]),e("tr",null,[e("td",{"data-sort-value":"In-place cache reuse"},"In-place cache reuse"),e("td",{"data-sort-value":"Optional"},[e("span",{class:"rk-support rk-support-partial"},"Optional")]),e("td",{"data-sort-value":"Single-output operation support and runtime types determine allocation behavior"},"Single-output operation support and runtime types determine allocation behavior")]),e("tr",null,[e("td",{"data-sort-value":"Reactive object field methods"},"Reactive object field methods"),e("td",{"data-sort-value":"Yes, declared grammar"},[e("span",{class:"rk-support rk-support-yes"},"Yes, declared grammar")]),e("td",{"data-sort-value":"No let, exceptions, comprehensions/generators, closures, do, or nested functions inside rewritten methods"},"No let, exceptions, comprehensions/generators, closures, do, or nested functions inside rewritten methods")]),e("tr",null,[e("td",{"data-sort-value":"Captured method branches/loops/recursion"},"Captured method branches/loops/recursion"),e("td",{"data-sort-value":"Yes, compiler subset"},[e("span",{class:"rk-support rk-support-yes"},"Yes, compiler subset")]),e("td",{"data-sort-value":"Exact source representation, ownership, effects, and overloads required"},"Exact source representation, ownership, effects, and overloads required")]),e("tr",null,[e("td",{"data-sort-value":"Functional free state transition"},"Functional free state transition"),e("td",{"data-sort-value":"Yes, narrow public subset"},[e("span",{class:"rk-support rk-support-yes"},"Yes, narrow public subset")]),e("td",{"data-sort-value":"Direct owned writes; exact map/copy/destructuring; bound numeric controls; captured finite Colon loops are statically unrolled"},"Direct owned writes; exact map/copy/destructuring; bound numeric controls; captured finite Colon loops are statically unrolled")]),e("tr",null,[e("td",{"data-sort-value":"Fixed-capacity structural container ABI"},"Fixed-capacity structural container ABI"),e("td",{"data-sort-value":"Yes, compiler subset"},[e("span",{class:"rk-support rk-support-yes"},"Yes, compiler subset")]),e("td",{"data-sort-value":"Prototype-bound recursive topology, exact primitive leaves, static authorities and owned aliases; validated read/write/copy/swap/select; no resizing or arbitrary elements"},"Prototype-bound recursive topology, exact primitive leaves, static authorities and owned aliases; validated read/write/copy/swap/select; no resizing or arbitrary elements")]),e("tr",null,[e("td",{"data-sort-value":"Recursive functional state-machine SCC"},"Recursive functional state-machine SCC"),e("td",{"data-sort-value":"No"},[e("span",{class:"rk-support rk-support-no"},"No")]),e("td",{"data-sort-value":"Finite containers and bounded control are admitted first; recursive SCC lowering rejects explicitly at the current frontier"},"Finite containers and bounded control are admitted first; recursive SCC lowering rejects explicitly at the current frontier")]),e("tr",null,[e("td",{"data-sort-value":"Infer effects from arbitrary Julia methods"},"Infer effects from arbitrary Julia methods"),e("td",{"data-sort-value":"No"},[e("span",{class:"rk-support rk-support-no"},"No")]),e("td",{"data-sort-value":"Source capture plus exact registrations only; no Julia IR/inference analysis"},"Source capture plus exact registrations only; no Julia IR/inference analysis")]),e("tr",null,[e("td",{"data-sort-value":"Generic effectful operation fallback"},"Generic effectful operation fallback"),e("td",{"data-sort-value":"No"},[e("span",{class:"rk-support rk-support-no"},"No")]),e("td",{"data-sort-value":"Opaque or specialization-unsafe calls reject before executable lowering"},"Opaque or specialization-unsafe calls reject before executable lowering")]),e("tr",null,[e("td",{"data-sort-value":"Runtime graph mutation after preparation"},"Runtime graph mutation after preparation"),e("td",{"data-sort-value":"No"},[e("span",{class:"rk-support rk-support-no"},"No")]),e("td",{"data-sort-value":"Cached stateless entries version out; compiled reactive programs throw version errors"},"Cached stateless entries version out; compiled reactive programs throw version errors")]),e("tr",null,[e("td",{"data-sort-value":"Reentrant/concurrent stateful kernel instance"},"Reentrant/concurrent stateful kernel instance"),e("td",{"data-sort-value":"No"},[e("span",{class:"rk-support rk-support-no"},"No")]),e("td",{"data-sort-value":"Use one prepared mutable/cache-owning instance per independent caller"},"Use one prepared mutable/cache-owning instance per independent caller")]),e("tr",null,[e("td",{"data-sort-value":"PPL/model semantics"},"PPL/model semantics"),e("td",{"data-sort-value":"No compiler feature"},[e("span",{class:"rk-support rk-support-no"},"No compiler feature")]),e("td",{"data-sort-value":"Examples build log densities and samplers from ordinary recipes; there is no trace/address language"},"Examples build log densities and samplers from ordinary recipes; there is no trace/address language")]),e("tr",null,[e("td",{"data-sort-value":"NUTS, log-density, or PPL domain API"},"NUTS, log-density, or PPL domain API"),e("td",{"data-sort-value":"No"},[e("span",{class:"rk-support rk-support-no"},"No")]),e("td",{"data-sort-value":"These live in external compilation examples and are not loaded or exported by RK"},"These live in external compilation examples and are not loaded or exported by RK")]),e("tr",null,[e("td",{"data-sort-value":"General dynamic scheduling in a hot kernel"},"General dynamic scheduling in a hot kernel"),e("td",{"data-sort-value":"No"},[e("span",{class:"rk-support rk-support-no"},"No")]),e("td",{"data-sort-value":"Dynamic planning exists only in ReactiveState demand orchestration"},"Dynamic planning exists only in ReactiveState demand orchestration")]),e("tr",null,[e("td",{"data-sort-value":"General Julia compiler replacement"},"General Julia compiler replacement"),e("td",{"data-sort-value":"No"},[e("span",{class:"rk-support rk-support-no"},"No")]),e("td",{"data-sort-value":"Unsupported captured shapes reject; ordinary opaque stateless recipes remain ordinary Julia"},"Unsupported captured shapes reject; ordinary opaque stateless recipes remain ordinary Julia")])])])])],-1)),t[3]||(t[3]=e("h2",{id:"How-to-read-a-rejection",tabindex:"-1"},[a("How to read a rejection "),e("a",{class:"header-anchor",href:"#How-to-read-a-rejection","aria-label":'Permalink to "How to read a rejection {#How-to-read-a-rejection}"'},"​")],-1)),t[4]||(t[4]=e("p",null,[a("Rejection is part of the compiler contract, not a request to guess at hidden semantics. A "),e("code",null,"PlanningError"),a(" says the dataflow query is impossible or cyclic. An authoring error says the declared graph/signature is malformed. A source-compiler rejection says captured syntax, callee identity, effect, ownership, concrete specialization domain, or currentness could not be proved. A compiled-state error says the fixed graph/program/handle boundary was violated.")],-1)),t[5]||(t[5]=e("p",null,"None of those paths silently switches to dynamic interpretation. If a computation belongs behind an opaque stateless recipe, put it there explicitly. If its mutations must participate in the source compiler, register the exact effect contract and remain within the corresponding concrete domain. If a reactive method needs unsupported lexical or exceptional control flow, keep that logic in ordinary Julia and pass its result through a declared port.",-1)),t[6]||(t[6]=e("p",null,[a("The "),e("a",{href:"./api"},"API reference"),a(" lists exported names; this page defines the algorithmic meaning and limits behind them.")],-1))])}const x=i(m,[["render",b]]);export{k as __pageData,x as default};
