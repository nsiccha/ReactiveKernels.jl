import{_ as a,o as t,c as r,ap as n}from"./chunks/framework.Dv1_Li4I.js";const u=JSON.parse('{"title":"Core constraints","description":"","frontmatter":{},"headers":[],"relativePath":"constraints.md","filePath":"constraints.md","lastUpdated":null}'),o={name:"constraints.md"};function i(s,e,c,l,d,h){return t(),r("div",null,[...e[0]||(e[0]=[n('<h1 id="Core-constraints" tabindex="-1">Core constraints <a class="header-anchor" href="#Core-constraints" aria-label="Permalink to &quot;Core constraints {#Core-constraints}&quot;">​</a></h1><p>These constraints apply to every ReactiveKernels lowering, extension, and example package. They also apply when data is bound during preparation. Backend limitations must be reported explicitly; they do not relax these rules.</p><h2 id="Preserve-data-dependent-iteration" tabindex="-1">Preserve data-dependent iteration <a class="header-anchor" href="#Preserve-data-dependent-iteration" aria-label="Permalink to &quot;Preserve data-dependent iteration {#Preserve-data-dependent-iteration}&quot;">​</a></h2><p><strong>Do not statically unroll a loop whose trip count derives from data.</strong> This includes runtime inputs, bound arrays, shapes, subject or observation counts, operation tables, ragged ranges, dose counts, and capacities computed from any of them. Knowing a length at preparation or trace time does not make it a structural constant. For example, expanding one statement per bit of the largest bound dose count is forbidden just as expanding one statement per subject is.</p><p>Retain a runtime loop, scan, or equivalent backend control-flow operation with explicit carried state and output buffers. Shapes may specialize an executable; the lowering must not duplicate the body according to those shapes or values. Native execution retains ordinary iteration. Fixed structural algebra, such as the scalar entries of an intrinsically three-compartment operator, is distinct from data-derived iteration and may be expanded.</p><h2 id="Preserve-lazy-branches" tabindex="-1">Preserve lazy branches <a class="header-anchor" href="#Preserve-lazy-branches" aria-label="Permalink to &quot;Preserve lazy branches {#Preserve-lazy-branches}&quot;">​</a></h2><p><strong>Do not replace required lazy control flow with eager <code>ifelse</code>, masked evaluation of both branches, or equivalent predication to bypass a compiler failure.</strong> An inactive branch must remain inactive, including its indexing, allocation, mutation, potentially undefined arithmetic, and derivative work. Clamping indices or inventing dummy buffers does not make such a rewrite valid.</p><p>Ordinary elementwise selection between already valid values remains a selection; it does not authorize evaluating an otherwise inactive computation. Preserve the authored Julia branch and loop semantics in both primal and derivative execution. If a backend cannot express them, report the unsupported case and isolate the backend failure instead of adopting an eager workaround.</p><p>When a plate cell&#39;s branch condition reads bound data only, preparation evaluates it per lane and splits the plate into one plate per taken arm (see the <a href="./compiler">compiler</a>, &quot;Data-bound branches in plate cells&quot;). This is the lazy semantics made structural: each lane still evaluates only its own arm, and no backend receives the branch.</p><h2 id="Derivative-rules-come-from-one-mathematical-graph,-never-by-hand" tabindex="-1">Derivative rules come from one mathematical graph, never by hand <a class="header-anchor" href="#Derivative-rules-come-from-one-mathematical-graph,-never-by-hand" aria-label="Permalink to &quot;Derivative rules come from one mathematical graph, never by hand {#Derivative-rules-come-from-one-mathematical-graph,-never-by-hand}&quot;">​</a></h2><p><strong>Do not write backend-specific derivative rules as durable code, and never attach a rule to a function this repository does not own.</strong> The user-ratified policy (ReactiveKernels:reactant decision <code>2026-09-14T13-13-44-484-15lmrss</code>, page <a href="./manual-derivative-rules">derivative rules</a> with <code>examples/manual_derivative_rule.jl</code>) is that a numerical primitive&#39;s derivatives are authored once as an ordinary pure-math <code>@kernel</code> graph — the stable primal plus named partials, or forward and reverse branches as outputs of one multi-output graph — and that every AD-protocol adapter (ChainRules, Mooncake, Enzyme, Reactant once its upstream bridge exists) is <em>generated</em> from activity-selected cuts of that graph and attached to a callable this repository owns. Separately authored JVP/VJP declarations, hand-written <code>EnzymeRules</code>/ChainRules methods, function or runtime-activity annotations, and finite-difference substitutions are rejected as durable code; the acceptance corpus differentiates the authored primal with the backend&#39;s ordinary reverse mode and nothing else. A rule on a foreign function such as <code>SpecialFunctions.loggamma</code> is type piracy on top of that: it silently changes every Enzyme user in the session.</p><p>The generator is shipped (<code>src/derivative_rules.jl</code>): <code>scalar_derivative_rule</code> turns a scalar graph that authors the primal plus one named partial per input, and <code>derivative_rule</code> a graph that authors a forward branch, a reverse branch, or both over array or scalar ports, into an RK-owned callable. The Enzyme, ChainRules and Mooncake adapters (<code>ext/ReactiveKernelsEnzymeExt.jl</code>, <code>ext/ReactiveKernelsChainRulesCoreExt.jl</code>, <code>ext/ReactiveKernelsMooncakeExt.jl</code>) derive every direction from the activity-selected cuts of that graph. The rules in package source are DistributionKernels&#39; <code>loggamma</code> and <code>logbeta</code> plus ReactiveKernelsPPL&#39;s <code>rk_expm</code>; the ODE backsolve adjoint consumes a caller&#39;s <code>DerivativeRule</code> right-hand side. Reverse-mode adapters stage each rule in two cuts whose residuals come from cross-stage liveness, so a shared intermediate is retained rather than recomputed. Rule cuts already trace under Reactant as plain mathematics. Emitting them as EnzymeMLIR custom rules is gated upstream on <a href="https://github.com/EnzymeAD/Enzyme/pull/2516" target="_blank" rel="noreferrer">EnzymeAD/Enzyme#2516</a>, which no Reactant release carries yet. ReactiveKernels generates that adapter once a release carries the mechanism.</p><p>A new backend failure of the ordinary path remains a backend limitation: isolate it with a backend-only reproducer under <code>benchmark/</code>, record it on this page, and, if it aborts the process, skip the affected acceptance cases by name until either the backend lowers the shape or a generated rule on an owned callable covers it.</p><h2 id="Acceptance-and-existing-limitations" tabindex="-1">Acceptance and existing limitations <a class="header-anchor" href="#Acceptance-and-existing-limitations" aria-label="Permalink to &quot;Acceptance and existing limitations {#Acceptance-and-existing-limitations}&quot;">​</a></h2><p>A lowering change must demonstrate that increasing relevant data lengths or capacities does not replicate loop bodies or control-flow regions. Check the generated backend structure as well as primal and AD parity with native Julia, including inactive branches and empty or ragged cases where supported. A small Julia statement count alone does not establish this: tracing can still expand a host loop.</p><p>These are required constraints, not a claim that every existing path already conforms. Every functional stateful method with authored control flow lowers through the retained control program, and host-drained observational records travel in its loop carry as structure-of-arrays storage written by one dynamic slot write per call site (the <a href="./compiler">compiler</a> page describes it), so no stateful path replicates a body per admitted iteration. The Reactant <a href="./scan">scan</a> lowering retains one <code>while</code> loop for every iterated-sequence shape, including bound host sequences.</p><p>The experimental rectangular PK path retains its loops and lazy branches but still fails reverse compilation. Its eager-branch and data-derived unrolling workarounds are not acceptable fixes. See the <a href="./scan">scan limitations</a>.</p><p>Three further backend limitations are isolated with standalone reproducers under <code>benchmark/</code> (the backend and its AD engine only, no ReactiveKernels code):</p><ul><li><p>A lazy branch inside a batched plate cell compiles and evaluates for every lane count, but reverse compilation through it fails once the batching pass realizes the plate as a loop (six lanes fail where four lanes, unrolled per lane, succeed): <code>repro_reactant_batch_if_reverse.jl</code>, still failing on Reactant 0.2.287. It concerns only branches whose condition reads a live value: a condition on bound data is split away during preparation and never reaches the backend. Plated support guards on live values therefore keep their authored branch and lose Reactant reverse gradients above that size until the batched-loop branch lowers upstream; native execution and native reverse are unaffected.</p></li><li><p>Reverse compilation through a retained <code>while</code> loop whose exit is data dependent (the adaptive ODE solver&#39;s <code>(n &lt; maxiters) &amp; (t &lt; t1)</code>) fails because the loop has no statically known iteration count: <code>repro_reactant_adaptive_while_reverse.jl</code>. The solver keeps the retained loop; its supported gradient is the backsolve adjoint, whose right-hand side is a <code>DerivativeRule</code> (the rule constraint above): the augmented system&#39;s vector-Jacobian products are the rule&#39;s authored reverse cut, evaluated inside the retained loop, so nothing differentiates anything.</p></li><li><p>Native Enzyme reverse mode aborts the process (an LLVM assertion in its shadow-allocation caching, reached while it differentiates SpecialFunctions&#39; <code>logabsgamma</code> port) when lazily evaluated branches around <code>loggamma</code>/<code>logbeta</code> sit in non-inlined functions differentiated together, one inside a loop: <code>repro_enzyme_lgamma_branch.jl</code>. That is the shape of a guarded <code>logpdf</code> plus an observation plate. The lazy guards stay; the distribution sources call DistributionKernels&#39; own <code>loggamma</code>/<code>logbeta</code>, rules generated from their pure-math graphs (the rule constraint above), which makes them primitives for Enzyme, so the failing body is never differentiated.</p></li></ul>',19)])])}const m=a(o,[["render",i]]);export{u as __pageData,m as default};
