import{_ as e,o as a,c as i,ao as t}from"./chunks/framework.DM2KPRmX.js";const k=JSON.parse('{"title":"Non-allocating kernels","description":"","frontmatter":{},"headers":[],"relativePath":"nonallocating.md","filePath":"nonallocating.md","lastUpdated":null}'),n={name:"nonallocating.md"};function l(h,s,r,o,p,d){return a(),i("div",null,[...s[0]||(s[0]=[t(`<h1 id="Non-allocating-kernels" tabindex="-1">Non-allocating kernels <a class="header-anchor" href="#Non-allocating-kernels" aria-label="Permalink to &quot;Non-allocating kernels {#Non-allocating-kernels}&quot;">​</a></h1><aside class="rk-review-state rk-review-state--review-pending" data-rk-review-state="review-pending" aria-label="Documentation review status"><div class="rk-review-state__icon" aria-hidden="true">◌</div><div class="rk-review-state__content"><div class="rk-review-state__heading"><span class="rk-review-state__badge">Review pending</span><span class="rk-review-state__eyebrow">Documentation status</span></div><strong class="rk-review-state__title">Implementation retained; guide awaiting review</strong><p>The non-allocating implementation and its tests remain in place, but this guide has not yet received the current focused review. This label records documentation review status, not a known runtime failure.</p></div></aside><p><code>prepare_nonallocating</code> changes how a kernel runs, not what it computes. It ships as an optional extension, so the base ReactiveKernels package keeps no hard dependency on MutatingFunctions (which is not registered yet) and still installs on its own on Julia 1.10.</p><p>Install the reviewed MutatingFunctions revision and load both packages to activate the extension:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Pkg</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Pkg</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">add</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(url </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;https://github.com/nsiccha/MutatingFunctions.jl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        rev </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;b353559ef3e391ae2e2d98256b6967903fdfa410&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ReactiveKernels, MutatingFunctions</span></span></code></pre></div><p>It picks exactly the same recipes as <a href="./api"><code>prepare</code></a>; the only difference is that each operation writes its result into a reusable buffer the kernel keeps (via <code>MutatingFunctions.apply!!</code>), instead of allocating a fresh result on every call.</p><h2 id="Prepare,-warm,-reuse" tabindex="-1">Prepare, warm, reuse <a class="header-anchor" href="#Prepare,-warm,-reuse" aria-label="Permalink to &quot;Prepare, warm, reuse {#Prepare,-warm,-reuse}&quot;">​</a></h2><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ReactiveKernels, MutatingFunctions</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@kernel</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> g</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Vector{Float64}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> begin</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    copied</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Vector{Float64}</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> copy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    reversed</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Vector{Float64}</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> reverse</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(copied)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">p </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> plan</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(g)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">k </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> prepare_nonallocating</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(p)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">k</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]) </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># first call allocates and seeds both caches</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">y </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> k</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">])   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># later calls reuse them</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> y </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><p>Here <code>y</code> is storage the kernel owns, and a later call overwrites that same array. This is not guaranteed for every operation: each cache keeps whatever its operation first returns, so an operation that just passes an input through (like <code>identity</code>) may hold onto a caller&#39;s array, and a plan with no recipes returns its <code>have</code> value directly. So treat every mutable result as borrowed — it may share memory with an input or be overwritten on the next call — and copy it if it has to outlive that next call.</p><h2 id="What-changes-in-the-generated-code" tabindex="-1">What changes in the generated code <a class="header-anchor" href="#What-changes-in-the-generated-code" aria-label="Permalink to &quot;What changes in the generated code {#What-changes-in-the-generated-code}&quot;">​</a></h2><p>The ordinary generated function calls each operation directly:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">copied </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __ops__[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">](x)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">reversed </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __ops__[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">](copied)</span></span></code></pre></div><p>The non-allocating version routes each call through a small helper that fills a reusable cache:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">copied </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> __cache_apply__</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(__caches__[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], __ops__[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], x)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">reversed </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> __cache_apply__</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(__caches__[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], __ops__[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], copied)</span></span></code></pre></div><p><code>code_expr(k)</code> shows the resulting function. The recipes chosen and the order of recipes, inputs, and outputs are all unchanged. Custom <code>passes</code> see the ordinary function first; the cache rewrite always runs last.</p><h2 id="Fused-authored-sources-decompose-into-destination-passing-steps" tabindex="-1">Fused authored sources decompose into destination-passing steps <a class="header-anchor" href="#Fused-authored-sources-decompose-into-destination-passing-steps" aria-label="Permalink to &quot;Fused authored sources decompose into destination-passing steps {#Fused-authored-sources-decompose-into-destination-passing-steps}&quot;">​</a></h2><p>An operation captured from <code>@kernel</code> source (for example <code>W * transpose(X) .+ b</code> or <code>vcat(zeros(1, n), m)</code>) is a single fused callable, and no in-place method can exist for an arbitrary closure. Instead of caching such a recipe as one opaque operation, the rewrite decomposes its captured expression into primitive steps at preparation time:</p><ul><li><p>identity-preserving wrappers (<code>view</code>, <code>reshape</code>, <code>transpose</code>, <code>eachcol</code>, ranges, scalar arithmetic) run inline — they never owned a buffer worth caching;</p></li><li><p>broadcast materializations (dotted calls, and array <code>getindex</code> with range indices), <code>vcat</code>, <code>zeros</code>/<code>ones</code>, and matrix products each become their own destination-passing step with a typed persistent cache, guarded by exact shape and eltype checks so a batch-size change reseeds instead of corrupting a stale buffer;</p></li><li><p>every other call becomes its own cache step, so registered <code>apply!!</code> coverage applies per step.</p></li></ul><p>Free symbols in the captured expression resolve against the authoring module&#39;s own <code>const</code> bindings — the exact functions the fused closure would call, never name-based guesses. Any source shape outside this grammar (control flow, a non-<code>const</code> global, a call through a port) keeps the whole-recipe cache step, which preserves the original closure semantics unchanged.</p><p>One observable difference: a plate reduction such as <code>sum</code> over an authored plate is fused into an accumulator loop by ordinary <code>prepare</code>, while the non-allocating kernel materializes the pointwise plate into its cache and then sums it. The materialized total is bit-exactly <code>sum</code> of the pointwise values; against the fused accumulation, only floating-point summation association differs.</p><h2 id="Allocation-contract" tabindex="-1">Allocation contract <a class="header-anchor" href="#Allocation-contract" aria-label="Permalink to &quot;Allocation contract {#Allocation-contract}&quot;">​</a></h2><p>The first call has no cache yet, so it runs the ordinary allocating operation and stores the result. Later calls hand that stored result back for the operation to overwrite in place.</p><p>So for a warmed-up call to allocate nothing, every operation needs an allocation-free <code>apply!!</code> method for the actual cache and argument types. MutatingFunctions&#39; generic fallback keeps the right result but may still allocate a temporary. Measure through a function barrier after warm-up:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> allocations</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(k, x)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    k</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    @allocated</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> k</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> allocations</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(k, [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span></span></code></pre></div><p>This first version is deliberately narrow:</p><ul><li><p>every selected recipe must have exactly one output, because a cache holds one result;</p></li><li><p>each cache keeps whatever its operation first returns; an ordinary allocating operation gives fresh storage the kernel then owns, an operation that just passes an input through (like <code>identity</code>) may keep a caller&#39;s array, and a plan with no recipes returns its inputs directly;</p></li><li><p>a prepared kernel holds this mutable state, so it is not safe to call from two tasks at once; prepare one kernel per independent caller;</p></li><li><p>for a generic cache step, resizing and shape changes are supported only as far as the chosen <code>apply!!</code> methods support them; the decomposed destination steps (broadcast, <code>vcat</code>, <code>zeros</code>/<code>ones</code>, matrix product) always guard shape and eltype and reseed on a mismatch.</p></li></ul><p>These are limits on how the kernel <em>runs</em>, not on what it computes: the graph is unchanged, and the same <code>Plan</code> can always be given to ordinary <code>prepare</code> instead.</p><h2 id="Reactive-layers-use-owned-state,-not-borrowed-caches" tabindex="-1">Reactive layers use owned state, not borrowed caches <a class="header-anchor" href="#Reactive-layers-use-owned-state,-not-borrowed-caches" aria-label="Permalink to &quot;Reactive layers use owned state, not borrowed caches {#Reactive-layers-use-owned-state,-not-borrowed-caches}&quot;">​</a></h2><p><code>prepare_nonallocating</code> is meant for calling one kernel directly, over and over: each cache is <em>borrowed</em> — it may share memory with an input and is overwritten on the next call. The reactive layers deliberately do <strong>not</strong> use it. <a href="./online-stats"><code>ReactiveState</code></a> keeps computed, frozen, and checkpointed values around and reuses them later, so a borrowed cache overwritten by a subsequent call would silently corrupt one of those saved values; <code>get!</code> therefore uses ordinary <a href="./api"><code>prepare</code></a>-style kernels, whose results the state owns outright.</p><p>If you want in-place updates <em>and</em> the reactive machinery, use <code>prepare_reactive</code> → <code>CompiledReactiveState</code>: <code>mutate!</code>/<code>touch!</code> edit the declared mutable inputs in place, and derived values live in buffers the state owns. Ownership, invalidation, and freeze/checkpoint all still work; for the public object/method form over the same state machinery, see <a href="/ReactiveKernels.jl/dev/online-stats#stateful-welford-moments">Stateful Welford moments</a>. Reach for a <code>prepare_nonallocating</code> kernel only for direct, single-caller use, and copy any mutable result you need to keep before the next call.</p><h2 id="Reproducing-the-extension-tests" tabindex="-1">Reproducing the extension tests <a class="header-anchor" href="#Reproducing-the-extension-tests" aria-label="Permalink to &quot;Reproducing the extension tests {#Reproducing-the-extension-tests}&quot;">​</a></h2><p>The optional extension is tested against the exact reviewed MutatingFunctions revision used above:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --startup-file=no</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test/run_nonallocating_integration.jl</span></span></code></pre></div><p>This creates a temporary consumer environment, installs MutatingFunctions from the public URL at the pinned commit, develops the current ReactiveKernels tree, and runs the allocation/API tests.</p>`,34)])])}const g=e(n,[["render",l]]);export{k as __pageData,g as default};
