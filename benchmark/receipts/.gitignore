all80-native.toml
all80-reactant.toml
# Runtime byproducts (like the phase receipts above): per-run content-addressed failure diagnostics
# and receipt backups. The aggregated receipt carries the diagnostic strings + content-address
# digests; the .log files themselves are reproducible byproducts, not tracked deliverables.
diagnostics/
*.prefix-backup-*
