# Repository constraints

Read [Core constraints](docs/src/constraints.md) before changing a lowering,
extension, or example. These repository-wide requirements apply to native and
compiled execution, including bound data. Existing nonconforming paths are
limitations to fix or reject, not precedents that waive the constraints.
